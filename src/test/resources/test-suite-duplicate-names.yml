---
name: "Dup Suite"
rest-client:
  connect-timeout: 5000
tests:
- name: "dup"
  request:
    method: "GET"
    url: "http://x.com"
  assertions:
  - type: "status_code"
    expected: 200
- name: "dup"
  request:
    method: "GET"
    url: "http://x.com"
  assertions:
  - type: "status_code"
    expected: 200
