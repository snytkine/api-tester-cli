---
name: "Suite with rest_client default headers"
rest-client:
  headers:
    X-Api-Key: "key-123"
tests:
- name: "GET with suite headers"
  request:
    method: "GET"
    url: "/objects"
  assertions:
  - type: "status_code"
    expected: 200
