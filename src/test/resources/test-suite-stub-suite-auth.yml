---
name: "Suite with rest_client basic auth"
rest_client:
  auth:
    type: "basic"
    username: "admin"
    password: "secret"
tests:
- name: "GET with suite auth"
  request:
    method: "GET"
    url: "/objects"
  assertions:
  - type: "status_code"
    expected: 200
