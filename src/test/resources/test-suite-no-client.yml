---
name: "Suite without a rest client"
description: "Fixture that declares neither rest-client nor rest-clients."
variables:
  api_base_url: "https://api.restful-api.dev"
tests:
- name: "Objects Test"
  request:
    method: "GET"
    url: "[[${suite.api_base_url}]]/objects"
    headers:
      Accept: "application/json"
  assertions:
  - type: "status_code"
    expected: 200
