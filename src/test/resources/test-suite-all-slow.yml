---
name: "All Slow Suite"
rest-client:
  connect-timeout: 5000
tests:
- name: "slow test one"
  tag: "slow"
  request:
    method: "GET"
    url: "http://example.com"
  assertions:
  - type: "status_code"
    expected: 200
- name: "slow test two"
  tag: "slow"
  request:
    method: "GET"
    url: "http://example.com"
  assertions:
  - type: "status_code"
    expected: 200
